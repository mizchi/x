# Tests intentionally excluded from coverage check.
# These test upstream internals not exposed by the mizchi/x wrapper.
# Format: package:test_name # reason

# --- http ---
# HTTP parser internals (not exposed)
http:read_request basic
http:read_request chunked
http:read_request fixed body
http:read_request stream
http:read_response basic
http:multiple request
# HTTP sender internals (not exposed)
http:send_request basic
http:send_request stream
http:send_request stream2
http:send invalid header
# Proxy feature (not exposed)
http:passthrough mode
http:passthrough mode remaining data
http:proxied http request
http:proxied https request
http:proxied misc request
http:proxy error
# URL resolver internals (wrapper has its own extract_host_url/extract_path_from_url tests)
http:resolve_url
# Streaming API (wrapper exposes simple get/post/put, not get_stream/post_stream/put_stream)
http:request streaming

# --- websocket ---
# Low-level frame handling (not exposed - wrapper uses recv/send only)
websocket:bad client
websocket:write buffering
websocket:single frame multiple read
websocket:ignored message
# Ping/pong internals (wrapper delegates to upstream, not independently testable)
websocket:send ping
websocket:duplicated ping
websocket:multiple ping
websocket:ping auto-reply
websocket:ping auto-reply race-with-write
websocket:ping cancelled
websocket:ping inside message
websocket:pong ignored
# UTF-8 validation internals (handled by upstream)
websocket:UTF8 validation
websocket:UTF8 validation large message
websocket:validate utf8 single byte
websocket:validate utf8 two bytes
websocket:validate utf8 three bytes
websocket:validate utf8 four bytes
websocket:validate utf8 in the middle
# Upstream integration tests with different API shape
websocket:websocket basic
websocket:close test
websocket:large message
websocket:external client
websocket:CloseCode conversions
