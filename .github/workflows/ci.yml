name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install MoonBit CLI
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"

      - name: Moon version
        run: moon version --all

      - name: Moon update (mizchi/x)
        run: moon update

      - name: Moon update (mizchi/fswatch)
        working-directory: fswatch
        run: moon update

      - name: Check format (mizchi/x)
        run: moon fmt --check

      - name: Check format (mizchi/fswatch)
        working-directory: fswatch
        run: moon fmt --check

      - name: Check pkg.generated.mbti drift (mizchi/x)
        run: |
          moon info
          if ! git diff --exit-code -- '**/pkg.generated.mbti'; then
            echo "::error::pkg.generated.mbti (mizchi/x) is out of date. Run 'moon info' and commit the changes."
            exit 1
          fi

      - name: Check pkg.generated.mbti drift (mizchi/fswatch)
        working-directory: fswatch
        run: |
          moon info
          if ! git diff --exit-code -- '**/pkg.generated.mbti'; then
            echo "::error::pkg.generated.mbti (mizchi/fswatch) is out of date. Run 'moon info' and commit the changes."
            exit 1
          fi

  test:
    name: Test ${{ matrix.module }} (${{ matrix.os }}, ${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        target: [native, js]
        module: [x, fswatch]
        exclude:
          # mizchi/x has not been validated on Windows yet; scope the
          # Windows runner to fswatch (which is the cross-platform target).
          - module: x
            os: windows-latest
          # js target on Windows is also unvalidated for now.
          - target: js
            os: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        if: matrix.target == 'js'
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install MoonBit CLI (Unix)
        if: runner.os != 'Windows'
        run: |
          curl -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"

      - name: Install MoonBit CLI (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          irm https://cli.moonbitlang.com/install/powershell.ps1 | iex
          "$env:USERPROFILE\.moon\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Moon version
        run: moon version --all

      - name: Moon update
        run: moon update
        working-directory: ${{ matrix.module == 'x' && '.' || 'fswatch' }}

      - name: Moon check
        run: moon check --deny-warn --target ${{ matrix.target }}
        working-directory: ${{ matrix.module == 'x' && '.' || 'fswatch' }}

      - name: Moon test
        run: moon test --target ${{ matrix.target }}
        working-directory: ${{ matrix.module == 'x' && '.' || 'fswatch' }}
